---
phase: 01-wasm-engine-and-telemetry-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workers/pyodide.worker.ts
  - src/hooks/usePyodideWorker.ts
  - src/components/terminal/OutputPanel.tsx
  - src/App.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-02

must_haves:
  truths:
    - User code executes in an isolated Web Worker without blocking the UI
    - The Pyodide worker initializes successfully using CDN scripts
    - The worker is automatically terminated and respawned if a script takes too long (timeout)
    - Python execution output or errors are visible to the user
  artifacts:
    - path: src/workers/pyodide.worker.ts
      provides: Pyodide initialization and message handling
    - path: src/hooks/usePyodideWorker.ts
      provides: React lifecycle management and timeout logic for the worker
    - path: src/components/terminal/OutputPanel.tsx
      provides: UI component to display execution results/errors
  key_links:
    - from: src/hooks/usePyodideWorker.ts
      to: src/workers/pyodide.worker.ts
      via: Worker instantiation and postMessage communication
    - from: src/App.tsx
      to: src/hooks/usePyodideWorker.ts
      via: calling runCode upon user action
---

<objective>
Setup the Pyodide Web Worker environment and integrate it with the React application.

Purpose: Enable secure, non-blocking execution of user-submitted Python code in the browser with timeout protection against infinite loops.
Output: A functional worker, a React hook to orchestrate it, and UI wired to display execution output.
</objective>

<execution_context>
@/Users/vasconcellos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wasm-engine-and-telemetry-bridge/01-CONTEXT.md
@.planning/phases/01-wasm-engine-and-telemetry-bridge/01-RESEARCH.md
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pyodide Web Worker</name>
  <files>src/workers/pyodide.worker.ts</files>
  <action>
    Create a standard Web Worker that loads Pyodide from CDN (https://cdn.jsdelivr.net/pyodide/v0.25.0/full/pyodide.js).
    - Expose an `onmessage` handler accepting `init` and `run` commands.
    - `init`: call `loadPyodide()`, post back `ready`.
    - `run`: takes `code` string, executes it using `pyodide.runPythonAsync`, and posts back success (with return value if any) or error.
    Do NOT include `micropip` or open-telemetry packages yet; keep it minimal.
  </action>
  <verify>
    <automated>npm run tsc</automated>
    <manual>No direct UI test yet, pure TS compilation check.</manual>
    <sampling_rate>Run after this task commits.</sampling_rate>
  </verify>
  <done>Worker handles init and run messages properly and typescript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create Worker React Hook</name>
  <files>src/hooks/usePyodideWorker.ts</files>
  <action>
    Create a React hook `usePyodideWorker` to manage the worker's lifecycle.
    - Instantiate the worker using `new Worker(new URL('../workers/pyodide.worker.ts', import.meta.url), { type: 'module' })`.
    - Maintain state: `isReady`, `isRunning`, `output`.
    - Provide a `runCode(code: string, timeoutMs = 5000)` function that returns a Promise.
    - Implement timeout logic: If the worker doesn't respond within `timeoutMs`, call `worker.terminate()`, throw a timeout error, and re-instantiate the worker to ensure it's ready for the next run.
  </action>
  <verify>
    <automated>npm run tsc</automated>
    <manual>Review code for correct worker instantiation and timeout logic.</manual>
    <sampling_rate>Run after this task commits.</sampling_rate>
  </verify>
  <done>Hook exposes worker state and safe execution wrapper with robust termination logic.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Worker and Output Panel in App</name>
  <files>
    src/components/terminal/OutputPanel.tsx
    src/App.tsx
  </files>
  <action>
    - Create `OutputPanel.tsx` that displays a simple terminal-like view (black background, monospaced text) to show strings.
    - In `App.tsx`, import `usePyodideWorker`.
    - Render `OutputPanel` below or alongside the `ValidationPanel` (or temporarily replace the fake validation logic) in the instrumentation phase.
    - When the user clicks "Validate" (or a new "Run" button), call `runCode(code)`. Catch errors/timeouts and display them in the `OutputPanel`.
    - Maintain existing structural layout in `App.tsx`, just inject the output visualization and worker invocation.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Open the app, write `2 + 2` or `while True: pass` in the editor, click run, verify result or timeout in OutputPanel.</manual>
    <sampling_rate>Run after this task commits.</sampling_rate>
  </verify>
  <done>Python code executes in the browser and output/errors are visible in the UI.</done>
</task>

</tasks>

<verification>
- Run a basic python calculation in the app.
- Run an infinite loop (`while True: pass`) and verify the UI doesn't freeze and a timeout error is caught.
</verification>

<success_criteria>
- Worker handles Python execution safely in background.
- UI reacts to running state and displays results.
- Infinite loops are successfully terminated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-wasm-engine-and-telemetry-bridge/01-01-SUMMARY.md`
</output>
