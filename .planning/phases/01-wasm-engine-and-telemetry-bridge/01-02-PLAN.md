---
phase: 01-wasm-engine-and-telemetry-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/workers/python/setup_telemetry.py
  - src/workers/pyodide.worker.ts
  - src/hooks/usePyodideWorker.ts
  - src/App.tsx
autonomous: true
requirements:
  - CORE-03
  - CORE-04

must_haves:
  truths:
    - Pyodide automatically installs opentelemetry python SDK dependencies via micropip
    - Python environment is pre-configured with a custom SpanExporter and stdout override before user code runs
    - Emitted spans are serialized to JSON in Python and captured by the React hook
    - Standard print statements in Python are captured and surfaced to the UI
  artifacts:
    - path: src/workers/python/setup_telemetry.py
      provides: Python-side telemetry setup and standard output overriding
    - path: src/workers/pyodide.worker.ts
      provides: Dependency installation and setup script injection
    - path: src/hooks/usePyodideWorker.ts
      provides: Extended message handling for JSON spans and stdout events
  key_links:
    - from: src/workers/python/setup_telemetry.py
      to: src/workers/pyodide.worker.ts
      via: Evaluated as a raw string during worker initialization
    - from: src/hooks/usePyodideWorker.ts
      to: src/App.tsx
      via: Exposing captured telemetry data to the application layer
---

<objective>
Implement the OpenTelemetry Python-to-JS bridge to capture spans and standard output natively from the worker.

Purpose: Allow user's Python instrumentation code to generate real OpenTelemetry spans, serialize them safely to JSON, and deliver them to the React frontend for validation.
Output: A Python telemetry setup script, extended worker initialization, and telemetry state tracking in the frontend hook.
</objective>

<execution_context>
@/Users/vasconcellos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wasm-engine-and-telemetry-bridge/01-CONTEXT.md
@.planning/phases/01-wasm-engine-and-telemetry-bridge/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python Telemetry Setup Script</name>
  <files>src/workers/python/setup_telemetry.py</files>
  <action>
    Create a python script that will be injected before user code.
    - Implement `JSStdout` with a `write(self, s)` method that calls `js.postMessage(json.dumps({"type": "stdout", "message": s}))`. Assign it to `sys.stdout`.
    - Implement `JSSpanExporter(SpanExporter)`. In its `export` method, iterate `spans`, serialize each via `span.to_json()`, and call `js.postMessage(json.dumps({"type": "telemetry", "span": json.loads(span_json_str)}))`. Return `SpanExportResult.SUCCESS`.
    - Configure the global OpenTelemetry TracerProvider with a `SimpleSpanProcessor` wrapping the `JSSpanExporter`.
    Avoid any batching/threading.
  </action>
  <verify>
    <automated>test -f src/workers/python/setup_telemetry.py</automated>
    <manual>Review script for correct usage of `to_json` and `postMessage`.</manual>
    <sampling_rate>Run after this task commits.</sampling_rate>
  </verify>
  <done>Python file created with stdout mock and custom OpenTelemetry SpanExporter.</done>
</task>

<task type="auto">
  <name>Task 2: Inject OTel Dependencies and Setup</name>
  <files>src/workers/pyodide.worker.ts</files>
  <action>
    Update the worker `init` block to install OpenTelemetry packages and run the setup script.
    - After `loadPyodide`, import `micropip` and await `micropip.install(['opentelemetry-api', 'opentelemetry-sdk'])`.
    - Import `setup_telemetry.py` as a raw string (e.g., `import setupScript from './python/setup_telemetry.py?raw'`).
    - Execute `await pyodide.runPythonAsync(setupScript)` to register the environment BEFORE signaling `ready` back to the main thread.
    - Modify the `run` handler so that if user code generates stdout or telemetry via `postMessage`, it routes properly. Note that Pyodide allows exposing a python variable representing the JS global, or accessing the worker's postMessage natively via `import js; js.postMessage(...)`.
  </action>
  <verify>
    <automated>npm run tsc</automated>
    <manual>Review worker code for micropip installation and raw import injection.</manual>
    <sampling_rate>Run after this task commits.</sampling_rate>
  </verify>
  <done>Worker successfully installs OTel dependencies and applies the Python setup payload on initialization.</done>
</task>

<task type="auto">
  <name>Task 3: Capture Telemetry and Stdout in UI</name>
  <files>
    src/hooks/usePyodideWorker.ts
    src/App.tsx
  </files>
  <action>
    - Update `usePyodideWorker` to handle incoming messages of `type: "stdout"` and `type: "telemetry"`. 
    - Maintain state for `spans` (Array of parsed telemetry objects) and `stdout` (Array of strings). Clear these states on each new `runCode` invocation.
    - In `App.tsx`, pass the `stdout` from the hook to the `OutputPanel`.
    - For now, log the captured `spans` array to `console.log` in `App.tsx` and optionally render a count of captured spans in the OutputPanel so we can verify the telemetry bridge works end-to-end.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Run an OpenTelemetry "Hello World" span creation script in the app, confirm "stdout" is shown and spans are logged in the browser console.</manual>
    <sampling_rate>Run after this task commits.</sampling_rate>
  </verify>
  <done>Telemetry correctly flows from Python WASM to React state, verifying the bridge.</done>
</task>

</tasks>

<verification>
- Instrument a simple span in the code editor, click Run, and confirm the JSON span representation appears in the JS console and stdout appears in the panel.
</verification>

<success_criteria>
- `micropip` dynamically installs OpenTelemetry upon worker boot.
- JS-bridged spans rely strictly on `.to_json()` serialization to avoid PyProxy memory leaks.
- React app holds an array of cleanly parsed telemetry span objects.
</success_criteria>

<output>
After completion, create `.planning/phases/01-wasm-engine-and-telemetry-bridge/01-02-SUMMARY.md`
</output>
