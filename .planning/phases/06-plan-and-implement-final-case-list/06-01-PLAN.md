---
phase: 06-plan-and-implement-final-case-list
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cases/001-hello-span/case.yaml
  - src/cases/002-auto-magic/case.yaml
  - src/cases/003-the-collector/case.yaml
autonomous: true
requirements:
  - CASE-01
  - CASE-02
  - CASE-03

must_haves:
  truths:
    - "Case 001 (Hello, Span) description opens with a compelling production anecdote explaining why spans matter before the exercise"
    - "Case 002 (Auto-magic) description explains zero-code instrumentation and why it covers 100+ libraries before the exercise"
    - "Case 003 (The Collector) description explains the pipeline model (receivers → processors → exporters) before the YAML task"
    - "All 3 rewrites are concept-first: concept → production relevance → exercise, no prior OTel knowledge assumed"
    - "Existing validation rules and Phase 2 data are preserved unchanged"
  artifacts:
    - path: src/cases/001-hello-span/case.yaml
      provides: "Rewritten concept-first description for Hello Span"
      contains: "what is a span"
    - path: src/cases/002-auto-magic/case.yaml
      provides: "Rewritten concept-first description for Auto-magic"
      contains: "auto-instrumentation"
    - path: src/cases/003-the-collector/case.yaml
      provides: "Rewritten concept-first description for The Collector"
      contains: "receivers"
  key_links:
    - from: src/cases/001-hello-span/case.yaml
      to: src/lib/validation.ts
      via: "validations[*].type values"
      pattern: "type: (span_exists|attribute_exists|telemetry_flowing)"
---

<objective>
Rewrite the Phase 1 `description` fields of the three existing cases (Hello Span, Auto-magic, The Collector) to be concept-first. Each case currently jumps directly to the exercise; the rewrite adds a concept primer explaining what the OTel feature is and why it matters in production before presenting the task.

Purpose: Establish the learning-first flow that all 9 cases will follow. Cases 4-9 will model this pattern.
Output: Three updated `case.yaml` files with expanded, concept-first Phase 1 descriptions.
</objective>

<execution_context>
@/Users/vasconcellos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plan-and-implement-final-case-list/06-CONTEXT.md
@.planning/phases/06-plan-and-implement-final-case-list/06-RESEARCH.md

@src/cases/001-hello-span/case.yaml
@src/cases/002-auto-magic/case.yaml
@src/cases/003-the-collector/case.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Hello Span and Auto-magic descriptions</name>
  <files>src/cases/001-hello-span/case.yaml, src/cases/002-auto-magic/case.yaml</files>
  <action>
Rewrite the `phase1.description` field of both cases in-place. All other fields (validations, rootCauseOptions, phase2) are PRESERVED UNCHANGED.

**Case 001 - Hello, Span new description structure:**
1. Opening: "What is a Span?" — 2-3 sentences. A span represents a single unit of work. Every span has a name, start/end time, status, and key-value attributes. In production, spans let you see exactly where time is spent inside a request.
2. "Why it matters in production" — 1 production anecdote. Example: "Without spans, all you know is 'the API took 5 seconds.' With spans, you know 'the DB query waited 4.7s for a connection pool slot — increase pool size to fix.'"
3. "The Situation:" — keep existing content (order processing running slow, system is blind).
4. "Your Mission:" — keep existing content (create span around process_order, add order_id attribute).
5. "Key Concepts:" — expand to include: Span, TraceProvider, Tracer, Attribute, Context Manager (with block).
6. "Hints:" — keep existing numbered hints.

**Case 002 - Auto-magic new description structure:**
1. Opening: "What is Auto-instrumentation?" — 2-3 sentences. Auto-instrumentation patches popular libraries at import time so spans are generated without writing any OTel SDK calls. One line of code covers all HTTP calls, DB queries, and cache operations.
2. "Why it matters in production" — mention: covers 100+ Python libraries (urllib3, requests, SQLAlchemy, Redis, Celery...), zero-code spans for entire frameworks.
3. "The Situation:" — keep existing (payment service making external API calls, no visibility).
4. "Your Mission:" — keep existing.
5. "Key Concepts:" — add: Instrumentation library, Bootstrapper, Zero-code telemetry.
6. "Hints:" — keep existing.

Both rewritten descriptions should use Markdown (bold headers, bullet lists) since InstructionsPanel renders via ReactMarkdown.
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>Both case.yaml files have concept-first descriptions that open with "What is X?" before the exercise. All validation rules and Phase 2 data are bit-for-bit identical to the originals.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite The Collector description</name>
  <files>src/cases/003-the-collector/case.yaml</files>
  <action>
Rewrite the `phase1.description` field of case 003. All other fields are PRESERVED UNCHANGED.

**Case 003 - The Collector new description structure:**
1. Opening: "What is the OpenTelemetry Collector?" — 2-3 sentences. The Collector is a vendor-agnostic proxy that receives telemetry from your apps, processes it (filters, enriches, batches), and exports to one or more backends. It decouples your app from your observability backend.
2. "The Pipeline Model" — short section explaining: receivers (accept telemetry in various formats) → processors (transform/filter/batch spans) → exporters (send to backends like Jaeger, Prometheus, Grafana Tempo). Include a minimal ASCII diagram or bullet list showing the flow.
3. "Why it matters in production" — 1-2 sentences: Without a Collector, every service hardcodes an exporter endpoint. With a Collector, you change backends without touching app code.
4. "The Situation:" — keep existing (team just deployed Collector, need to configure tail sampling).
5. "Your Mission:" — keep existing (add tail_sampling to the processors section).
6. "Key Concepts:" — add: Collector, Pipeline, Receiver, Processor, Exporter, Tail Sampling, decision_wait.
7. "Hints:" — keep existing.

Rewritten description should use Markdown. Keep the same YAML structure (`type: yaml-config`, validations with `yaml_key_exists`, phase2 data unchanged).
  </action>
  <verify>
    <automated>npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>case 003 case.yaml has a concept-first description explaining the Collector pipeline model before the YAML exercise. All validation rules and Phase 2 data are unchanged.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run test -- --run` passes (no regressions)
2. `npm run dev` loads all 3 cases without console errors
3. Visually confirm InstructionsPanel shows concept primer text above "The Situation:" in each case
4. Validate case rules still work: running correct code for case 001 passes all 3 validations
</verification>

<success_criteria>
- All 3 case.yaml files have concept-first descriptions (concept → why it matters → exercise)
- Existing validation rules, hints, and Phase 2 data are preserved verbatim
- No TypeScript or test errors introduced
- Cases remain fully playable (Phase 1 and Phase 2 both functional)
</success_criteria>

<output>
After completion, create `.planning/phases/06-plan-and-implement-final-case-list/06-01-SUMMARY.md`
</output>
