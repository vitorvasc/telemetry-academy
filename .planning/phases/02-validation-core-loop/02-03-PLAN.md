---
phase: 02-validation-core-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/usePersistentState.ts
  - src/types/progress.ts
autonomous: true
requirements:
  - CORE-05
must_haves:
  truths:
    - User progress survives browser page refresh
    - User's code edits are preserved per case across refresh
    - localStorage has schema version for data migration
    - Auto-save triggers on every state change with 300ms debounce
    - "Reset All Progress" button clears all saved data
  artifacts:
    - path: src/hooks/usePersistentState.ts
      provides: Persistence hook with versioning
      exports: ["useAcademyState"]
    - path: src/types/progress.ts
      provides: PersistedState type definition
  key_links:
    - from: useAcademyState hook
      to: localStorage API
      via: JSON serialization with version check
    - from: PersistedState type
      to: App.tsx state structure
      via: type compatibility
---

<objective>
Implement localStorage persistence for user progress and code edits with schema versioning, debounced auto-save, and a reset mechanism.

Purpose: Enable users to resume their learning progress after browser refresh
Output: Working persistence layer with versioning and reset capability
</objective>

<execution_context>
@/Users/vasconcellos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/projetos/telemetry-academy/src/types/progress.ts
@/Users/vasconcellos/projetos/telemetry-academy/src/App.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Create persistence hook with versioning</name>
  <files>src/hooks/usePersistentState.ts</files>
  <action>
Create `src/hooks/usePersistentState.ts` implementing academy state persistence:

1. Define constants:
   - `STORAGE_KEY = 'telemetry-academy'`
   - `SCHEMA_VERSION = 1` (for future migrations)

2. Define PersistedState interface:
   ```typescript
   interface PersistedState {
     version: number;
     progress: CaseProgress[];
     caseCode: Record<string, string>; // caseId -> code
     timestamp: number;
   }
   ```

3. Implement `useAcademyState` hook:
   - Parameters: `initialProgress: CaseProgress[], initialCaseCode: Record<string, string>`
   - Returns: `{ progress, setProgress, caseCode, setCaseCode, resetAll, isLoaded }`

4. Implement load effect (client-side only):
   - useEffect runs once on mount
   - Try to load from localStorage
   - Check version matches SCHEMA_VERSION
   - If version mismatch or error, use initial values
   - Set `isLoaded = true` when complete
   - IMPORTANT: Only access localStorage in useEffect (hydration safety)

5. Implement auto-save with debounce:
   - useEffect watches progress and caseCode
   - Debounce 300ms (use setTimeout/clearTimeout)
   - Save to localStorage with current version and timestamp
   - Handle QuotaExceededError gracefully (console.warn)
   - Only save after isLoaded is true

6. Implement resetAll function:
   - Remove item from localStorage
   - Reset state to initial values
   - Returns to initial state as if first visit

7. Export hook and types.

Anti-patterns to avoid per RESEARCH.md:
- Don't sync on every render (use debounce)
- Don't access localStorage during SSR/render (only in useEffect)
- Always include version for schema migration
  </action>
  <verify>
    <automated>grep -q "export function useAcademyState" src/hooks/usePersistentState.ts && grep -q "SCHEMA_VERSION" src/hooks/usePersistentState.ts && grep -q "version:" src/hooks/usePersistentState.ts && echo "Hook created"</automated>
    <manual>Review hook has proper versioning and debounced save</manual>
  </verify>
  <done>
    - useAcademyState hook created with proper typing
    - Schema version constant defined (SCHEMA_VERSION = 1)
    - Load effect only runs client-side (in useEffect)
    - Auto-save debounced at 300ms
    - resetAll function clears localStorage and resets state
  </done>
</task>

<task type="auto">
  <name>Add PersistedState type to progress types</name>
  <files>src/types/progress.ts</files>
  <action>
Modify `src/types/progress.ts` to add PersistedState type:

1. Add PersistedState interface:
   ```typescript
   export interface PersistedState {
     version: number;
     progress: CaseProgress[];
     caseCode: Record<string, string>;
     timestamp: number;
   }
   ```

2. Verify CaseProgress interface has all needed fields:
   - caseId: string
   - status: CaseStatus
   - phase: 'instrumentation' | 'investigation' | 'complete'
   - attempts: number
   - timeStartedMs?: number
   - timeSolvedMs?: number
   - All fields should already exist, just verify

3. Export the new PersistedState type.

This type is used by the persistence hook and ensures type safety across the app.
  </action>
  <verify>
    <automated>grep -q "export interface PersistedState" src/types/progress.ts && echo "Type added"</automated>
    <manual>TypeScript compilation passes</manual>
  </verify>
  <done>
    - PersistedState interface exported from types/progress.ts
    - Type includes version, progress, caseCode, timestamp fields
  </done>
</task>

<task type="auto">
  <name>Integrate persistence into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Modify `src/App.tsx` to use persistent state:

1. Import useAcademyState hook:
   ```typescript
   import { useAcademyState } from './hooks/usePersistentState';
   ```

2. Replace state initialization:
   - Remove: `const [allProgress, setAllProgress] = useState(...)`
   - Remove: Code state management (will be handled by persistence)
   - Add: 
     ```typescript
     const { 
       progress, 
       setProgress, 
       caseCode, 
       setCaseCode, 
       resetAll, 
       isLoaded 
     } = useAcademyState(
       initProgress(cases),
       {} // initial caseCode empty
     );
     ```

3. Update switchCase function:
   - When switching cases, load saved code: `setCode(caseCode[id] || c.phase1.initialCode)`
   - Or initialize if no saved code

4. Add code auto-save effect:
   ```typescript
   useEffect(() => {
     if (isLoaded) {
       setCaseCode(prev => ({ ...prev, [currentCaseId]: code }));
     }
   }, [code, currentCaseId, isLoaded]);
   ```

5. Update all references:
   - Change `allProgress` → `progress`
   - Change `setAllProgress` → `setProgress`
   - Update `updateProgress` helper to use setProgress

6. Add Reset All Progress button:
   - Place in header or footer (Claude's discretion - perhaps settings menu or near case selector)
   - Use Trash2 or RotateCcw icon from lucide-react
   - Show confirmation dialog before reset
   - Call `resetAll()` on confirm
   - Reset should also reset attemptHistory

7. Handle loading state:
   - While `!isLoaded`, could show loading spinner or just render with initial state
   - Since SSR isn't an issue here, can render immediately

Ensure all state updates trigger auto-save via the hook's useEffect.
  </action>
  <verify>
    <automated>grep -q "useAcademyState" src/App.tsx && grep -q "resetAll" src/App.tsx && grep -q "isLoaded" src/App.tsx && echo "Persistence integrated"</automated>
    <manual>Review App.tsx uses persistent state hook</manual>
  </verify>
  <done>
    - App.tsx uses useAcademyState instead of useState for progress
    - Code auto-saves per case
    - Reset All Progress button implemented with confirmation
    - All state references updated
    - Loading state handled
  </done>
</task>

</tasks>

<verification>
- User refreshes page - progress and code are restored
- Schema version check works (old data cleared if version mismatch)
- Auto-save triggers on code changes (300ms debounce)
- Reset All Progress button works with confirmation
- localStorage quota errors handled gracefully
</verification>

<success_criteria>
- useAcademyState hook provides persistent progress and code
- Schema versioning enables future data migrations
- Debounced auto-save prevents excessive writes
- Reset functionality clears all saved data
- Progress survives browser refresh
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-core-loop/02-03-SUMMARY.md`
</output>
