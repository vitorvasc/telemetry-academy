---
phase: 02-validation-core-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useAcademyPersistence.ts
  - src/types/progress.ts
  - src/App.tsx
autonomous: true
requirements:
  - CORE-05
must_haves:
  truths:
    - User progress survives browser page refresh
    - User's code edits are preserved per case across refresh
    - localStorage has schema version for data migration
    - Auto-save triggers on every state change with 300ms debounce
    - Attempt history is persisted per case for progressive hints
    - "Reset All Progress" button clears all saved data
  artifacts:
    - path: src/hooks/useAcademyPersistence.ts
      provides: Persistence hook with versioning and attempt history
      exports: ["useAcademyPersistence"]
    - path: src/types/progress.ts
      provides: PersistedState type definition with attempt history
      exports: ["PersistedState", "CaseProgress"]
  key_links:
    - from: useAcademyPersistence hook
      to: localStorage API
      via: JSON serialization with version check
    - from: PersistedState type
      to: App.tsx state structure
      via: type compatibility
    - from: attemptHistory keyed by caseId
      to: validation engine
      via: passed in ValidationContext
---

<objective>
Implement localStorage persistence for user progress, code edits, and attempt history with schema versioning, debounced auto-save, and a reset mechanism. Per RESEARCH.md Pitfall 5, attempt history must be keyed by caseId to survive case switches.

Purpose: Enable users to resume their learning progress after browser refresh, with progressive hint attempt counts preserved
Output: Working persistence layer with versioning, attempt tracking, and reset capability
</objective>

<execution_context>
@/Users/vasconcellos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/projetos/telemetry-academy/src/types/progress.ts
@/Users/vasconcellos/projetos/telemetry-academy/src/App.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Create persistence hook with attempt history</name>
  <files>src/hooks/useAcademyPersistence.ts</files>
  <action>
Create `src/hooks/useAcademyPersistence.ts` implementing academy state persistence per RESEARCH.md patterns:

1. Define constants:
   - `STORAGE_KEY = 'telemetry-academy'`
   - `SCHEMA_VERSION = 1` (for future migrations)

2. Define PersistedState interface (will also add to types/progress.ts):
   ```typescript
   interface PersistedState {
     version: number;
     progress: CaseProgress[];
     caseCode: Record<string, string>; // caseId -> code
     attemptHistory: Record<string, Record<string, number>>; // caseId -> rule -> attempts
     timestamp: number;
   }
   ```

3. Implement `useAcademyPersistence` hook per RESEARCH.md:
   - Parameters: `initialProgress: CaseProgress[]`, `getInitialCode: (caseId: string) => string`
   - Returns: `{ progress, setProgress, caseCode, setCaseCode, attemptHistory, updateAttemptHistory, getAttemptCount, getSavedCode, saveCode, resetAll, isLoaded }`

4. Implement load effect (client-side only, per Pitfall 4 in RESEARCH.md):
   - useEffect runs once on mount
   - Try to load from localStorage
   - Check version matches SCHEMA_VERSION (per Pitfall 3)
   - If version mismatch or error, wipe and use initial values
   - Set `isLoaded = true` when complete
   - IMPORTANT: Only access localStorage in useEffect (hydration safety)

5. Implement auto-save with debounce (300ms, per RESEARCH.md):
   - useEffect watches progress, caseCode, attemptHistory
   - Debounce 300ms (use setTimeout/clearTimeout with useRef)
   - Save to localStorage with current version and timestamp
   - Handle QuotaExceededError gracefully (console.warn, per Pitfall 2)
   - Only save after isLoaded is true

6. Implement attempt history helpers:
   - `updateAttemptHistory(caseId, ruleDescription)` - increments attempt count
   - `getAttemptCount(caseId, ruleDescription)` - returns current attempt count
   - History keyed by caseId so switching cases preserves per-case attempts

7. Implement resetAll function:
    - Remove item from localStorage
    - Reset state to initial values
    - Returns to initial state as if first visit

8. Export hook and types.

Anti-patterns to avoid per RESEARCH.md:
- Don't sync on every render (use debounce)
- Don't access localStorage during SSR/render (only in useEffect)
- Always include version for schema migration
- Don't lose attempt history on case switch (key by caseId)
  </action>
  <verify>
    <automated>grep -q "export function useAcademyPersistence" src/hooks/useAcademyPersistence.ts && grep -q "SCHEMA_VERSION" src/hooks/useAcademyPersistence.ts && grep -q "attemptHistory" src/hooks/useAcademyPersistence.ts && grep -q "updateAttemptHistory" src/hooks/useAcademyPersistence.ts && echo "Hook created"</automated>
    <manual>Review hook has proper versioning, debounced save, and attempt history keyed by caseId</manual>
  </verify>
  <done>
    - useAcademyPersistence hook created with proper typing
    - Schema version constant defined (SCHEMA_VERSION = 1)
    - Load effect only runs client-side (in useEffect)
    - Auto-save debounced at 300ms
    - Attempt history keyed by caseId (per Pitfall 5)
    - updateAttemptHistory and getAttemptCount functions implemented
    - resetAll function clears localStorage and resets state
  </done>
</task>

<task type="auto">
  <name>Update PersistedState type in progress types</name>
  <files>src/types/progress.ts</files>
  <action>
Modify `src/types/progress.ts` to update PersistedState type with attempt history:

1. Update PersistedState interface (add attemptHistory field):
   ```typescript
   export interface PersistedState {
     version: number;
     progress: CaseProgress[];
     caseCode: Record<string, string>;
     attemptHistory: Record<string, Record<string, number>>; // caseId -> rule -> count
     timestamp: number;
   }
   ```

2. Verify CaseProgress interface has all needed fields:
   - caseId: string
   - status: CaseStatus ('locked' | 'available' | 'in-progress' | 'solved')
   - phase: 'instrumentation' | 'investigation' | 'complete'
   - attempts: number (investigation attempts)
   - timeStartedMs?: number
   - timeSolvedMs?: number
   - All fields should already exist, just verify

3. Ensure CaseStatus type is exported if defined separately.

4. Export PersistedState type if not already exported.

This type is used by the persistence hook and ensures type safety across the app. The nested Record for attemptHistory allows per-case, per-rule tracking.
  </action>
  <verify>
    <automated>grep -q "export interface PersistedState" src/types/progress.ts && grep -q "attemptHistory" src/types/progress.ts && echo "Type added"</automated>
    <manual>TypeScript compilation passes, PersistedState includes attemptHistory field</manual>
  </verify>
  <done>
    - PersistedState interface exported from types/progress.ts
    - Type includes version, progress, caseCode, attemptHistory, timestamp fields
    - attemptHistory is Record<caseId, Record<rule, count>> for per-case tracking
  </done>
</task>

<task type="auto">
  <name>Integrate persistence into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Modify `src/App.tsx` to use persistent state with attempt history:

1. Import useAcademyPersistence hook:
   ```typescript
   import { useAcademyPersistence } from './hooks/useAcademyPersistence';
   ```

2. Replace state initialization with persistence hook:
   - Remove: `const [allProgress, setAllProgress] = useState(...)`
   - Remove: Direct code state management for persistence (keep useState for current code)
   - Add: 
     ```typescript
     const { 
       progress: allProgress, 
       setProgress: setAllProgress, 
       getSavedCode,
       saveCode,
       attemptHistory,
       updateAttemptHistory,
       getAttemptCount,
       resetAll, 
       isLoaded 
     } = useAcademyPersistence(
       initProgress(cases),
       (caseId) => cases.find(c => c.id === caseId)?.phase1.initialCode || ''
     );
     ```

3. Initialize code from saved state in switchCase:
   - When switching cases: `setCode(getSavedCode(id) || c.phase1.initialCode)`
   - Update switchCase to use getSavedCode instead of always using initialCode

4. Add code auto-save effect:
   ```typescript
   useEffect(() => {
     if (isLoaded) {
       saveCode(currentCaseId, code);
     }
   }, [code, currentCaseId, isLoaded, saveCode]);
   ```

5. Update all references:
   - Change `allProgress` → already handled by destructuring
   - Change `setAllProgress` → already handled by destructuring
   - Update `updateProgress` helper to use setAllProgress

6. Add Reset All Progress button:
   - Place in header near CaseSelector or in footer (Claude's discretion)
   - Use Trash2 or RotateCcw icon from lucide-react
   - Show confirmation dialog before reset (two-step: click shows confirm/cancel)
   - Call `resetAll()` on confirm
   - Also need to reset local component state: setCode, setValidationResults, setInvestigationAttempts

7. Handle loading state:
   - While `!isLoaded`, show loading spinner
   - Render: `<div className="h-screen bg-slate-900 flex items-center justify-center text-white">Loading...</div>`

8. Pass attempt tracking to validation:
   - In handleValidate, get attempt history for current case to pass to validateSpans
   - After validation, update attempt counts for failed rules using updateAttemptHistory

Ensure all state updates trigger auto-save via the hook's useEffect. Per Pitfall 5 in RESEARCH.md, attempt history must survive case switches.
  </action>
  <verify>
    <automated>grep -q "useAcademyPersistence" src/App.tsx && grep -q "resetAll" src/App.tsx && grep -q "isLoaded" src/App.tsx && grep -q "attemptHistory" src/App.tsx && echo "Persistence integrated"</automated>
    <manual>Review App.tsx uses persistent state hook with attempt history tracking</manual>
  </verify>
  <done>
    - App.tsx uses useAcademyPersistence instead of useState for progress
    - Code auto-saves per case via saveCode
    - Code loads from saved state via getSavedCode
    - Attempt history integrated with validation flow
    - Reset All Progress button implemented with confirmation
    - Loading state handled (shows spinner while isLoaded is false)
  </done>
</task>

</tasks>

<verification>
- User refreshes page - progress, code, and attempt history are restored
- Schema version check works (old data cleared if version mismatch)
- Auto-save triggers on code changes (300ms debounce)
- Reset All Progress button works with confirmation
- localStorage quota errors handled gracefully
- Attempt history persists across case switches
</verification>

<success_criteria>
- useAcademyPersistence hook provides persistent progress, code, and attempt history
- Schema versioning enables future data migrations
- Debounced auto-save prevents excessive writes
- Reset functionality clears all saved data
- Progress survives browser refresh
- Attempt counts for progressive hints persist across case switches
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-core-loop/02-03-SUMMARY.md`
</output>
