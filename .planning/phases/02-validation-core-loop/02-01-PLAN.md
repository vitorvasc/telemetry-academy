---
phase: 02-validation-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validation.ts
  - src/types.ts
  - src/data/cases.ts
autonomous: true
requirements:
  - LOOP-02
  - LOOP-04
must_haves:
  truths:
    - User code runs and captures real spans via useCodeRunner
    - Validation engine checks captured span JSON objects (not code strings)
    - Validation rules check span existence, attributes, and structure
    - Progressive hints escalate after 3 failed attempts per rule
  artifacts:
    - path: src/lib/validation.ts
      provides: Span validation engine
      exports: ["validateSpans", "ValidationContext", "SpanCheck", "ValidationAttempt"]
    - path: src/types.ts
      provides: Extended ValidationRule with attemptHints
      contains: attemptHints, guidedMessage fields
  key_links:
    - from: validateSpans function
      to: captured spans array from useCodeRunner
      via: function parameter
    - from: ValidationRule type
      to: cases.ts validation definitions
      via: TypeScript type checking
---

<objective>
Build the span-based validation engine that checks real captured telemetry JSON objects against schema rules, replacing the current string-matching `simulateValidation()` approach.

Purpose: Enable real-time validation feedback on actual OpenTelemetry output, not code syntax
Output: Working validation engine with progressive hint support
</objective>

<execution_context>
@/Users/vasconcellos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/projetos/telemetry-academy/src/hooks/useCodeRunner.ts
@/Users/vasconcellos/projetos/telemetry-academy/src/types.ts
@/Users/vasconcellos/projetos/telemetry-academy/src/data/cases.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Create span validation engine in src/lib/validation.ts</name>
  <files>src/lib/validation.ts</files>
  <action>
Create a new file `src/lib/validation.ts` implementing the validation engine:

1. Define core types:
   - `SpanCheck` - validation check types: 'span_exists', 'attribute_exists', 'attribute_value', 'span_count'
   - `ValidationAttempt` - tracks attempt counts per rule
   - `ValidationContext` - holds spans array and attempt history
   - `ValidationResult` - extends ValidationRule with passed boolean, message, attemptsOnThisRule

2. Implement `validateSpans()` function:
   - Takes ValidationRule[], spans array, and attemptHistory Record
   - For each rule, check against captured spans using exact matching (no fuzzy)
   - Return ValidationResult[] with pass/fail status and appropriate message

3. Implement check helpers:
   - `checkSpanExists(rule, spans)` - returns true if any span matches rule.spanName
   - `checkAttributeExists(rule, spans)` - returns true if matching span has attributeKey
   - Support optional attributeValue matching for exact value checks

4. Implement progressive hint selection:
   - `selectMessage(rule, attempts)` - returns guidedMessage if attempts >= 3, else hintMessage or errorMessage
   - Default to hintMessage for first failures, escalate to guidedMessage after 3 attempts

5. Export all public types and functions.

Use native TypeScript pattern matching. Do NOT add Zod or other validation libraries per RESEARCH.md decision.
  </action>
  <verify>
    <automated>grep -q "export function validateSpans" src/lib/validation.ts && grep -q "SpanCheck" src/lib/validation.ts && echo "Validation engine created"</automated>
    <manual>Review src/lib/validation.ts has proper types and validateSpans function exported</manual>
  </verify>
  <done>
    - src/lib/validation.ts exists with validateSpans() function
    - All validation types exported (SpanCheck, ValidationContext, ValidationResult, ValidationAttempt)
    - checkSpanExists and checkAttributeExists helper functions implemented
    - Progressive message selection logic (guidedMessage after 3 attempts)
  </done>
</task>

<task type="auto">
  <name>Extend ValidationRule type with attempt hints</name>
  <files>src/types.ts</files>
  <action>
Modify `src/types.ts` to extend ValidationRule for progressive hints:

1. Add new optional fields to ValidationRule interface:
   - `hintMessage?: string` - Shown on first 1-2 failures
   - `guidedMessage?: string` - Shown after 3+ attempts (specific fix guidance)
   - `spanName?: string` - For span matching (may already exist, verify/ensure)
   - `attributeKey?: string` - For attribute matching (may already exist, verify/ensure)

2. Ensure ValidationResult interface includes:
   - `attemptsOnThisRule?: number` - Track attempts for progressive hint display
   - This field will be added by validation engine, ensure type supports it

3. Export any new types if needed.

Per locked decision: Progressive error messages start with hints and escalate to guided fixes after 3 failed attempts on the same check.
  </action>
  <verify>
    <automated>grep -q "hintMessage" src/types.ts && grep -q "guidedMessage" src/types.ts && grep -q "attemptsOnThisRule" src/types.ts && echo "Types extended"</automated>
    <manual>TypeScript compilation passes with no errors</manual>
  </verify>
  <done>
    - ValidationRule has hintMessage and guidedMessage optional fields
    - ValidationResult has attemptsOnThisRule field
    - Types compile without errors
  </done>
</task>

<task type="auto">
  <name>Add attempt hints to case validation rules</name>
  <files>src/data/cases.ts</files>
  <action>
Modify `src/data/cases.ts` to add progressive hint messages to existing validation rules:

For each validation rule in cases array, add:
1. `hintMessage` - friendly hint for first failures (e.g., "No span named 'process-order' found. Try using tracer.start_as_current_span()")
2. `guidedMessage` - specific fix guidance for 3+ attempts (e.g., "Use: with tracer.start_as_current_span('process-order') as span:")

Specific updates needed:
- hello-span-001 case:
  - span_exists rule: hint about start_as_current_span, guided with exact syntax
  - attribute_exists rule: hint about set_attribute, guided with exact code
  - telemetry_flowing rule: general hint about both span + attribute

- auto-magic-002 case:
  - span_exists rule: hint about auto-instrumentation configuration

Ensure all existing rule properties (type, description, successMessage, errorMessage) are preserved. Only ADD new hintMessage and guidedMessage fields.
  </action>
  <verify>
    <automated>grep -q "hintMessage" src/data/cases.ts && grep -q "guidedMessage" src/data/cases.ts && echo "Hints added to cases"</automated>
    <manual>Review cases.ts has progressive hints for all validation rules</manual>
  </verify>
  <done>
    - All validation rules in cases.ts have hintMessage field
    - All validation rules have guidedMessage field with specific fix code
    - Existing rule properties preserved
  </done>
</task>

</tasks>

<verification>
- Validation engine correctly checks span existence against captured spans
- Attribute validation works with nested span.attributes object
- Progressive messages escalate after 3 attempts
- TypeScript types compile without errors
</verification>

<success_criteria>
- src/lib/validation.ts exports working validateSpans() function
- Validation checks captured span JSON objects (not code strings)
- Progressive hint system ready (hintMessage → guidedMessage after 3 attempts)
- Case data updated with helpful progressive hints
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-core-loop/02-01-SUMMARY.md`
</output>
