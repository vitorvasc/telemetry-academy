---
phase: 02-validation-core-loop
plan: 02
type: execute
wave: 2
depends_on: ['02-01', '02-03']
files_modified:
  - src/App.tsx
  - src/components/ValidationPanel.tsx
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-04
must_haves:
  truths:
    - User clicks "Check Code" and sees spinner during execution
    - Validation results appear one by one with staggered animation
    - Validation checks real captured spans (not code strings)
    - All validations passing unlocks Investigation tab with celebration
    - Next case unlocks only after current case is fully solved
    - Code changes clear validation results (no stale state)
  artifacts:
    - path: src/App.tsx
      provides: Integrated validation flow
      contains: handleValidate using validateSpans, isValidating state
    - path: src/components/ValidationPanel.tsx
      provides: Spinner during execution, staggered reveal
  key_links:
    - from: App.tsx handleValidate
      to: validateSpans from src/lib/validation.ts
      via: import and function call
    - from: ValidationPanel
      to: isValidating state
      via: props
    - from: handleCaseSolved
      to: progress state update (next case unlock)
      via: updateProgress(nextCase.id, {status: 'available'})
---

<objective>
Integrate the validation engine into the UI flow, replacing `simulateValidation()` with real span-based validation. Add spinner feedback, staggered result reveals, celebration on completion, and proper case unlocking.

Purpose: Complete the core learning loop with real-time feedback and progression
Output: Fully working validation flow from code execution to case unlock
</objective>

<execution_context>
@/Users/vasconcellos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/vasconcellos/projetos/telemetry-academy/src/App.tsx
@/Users/vasconcellos/projetos/telemetry-academy/src/components/ValidationPanel.tsx
@/Users/vasconcellos/projetos/telemetry-academy/src/lib/validation.ts (from 02-01)
</execution_context>

<tasks>

<task type="auto">
  <name>Integrate validation engine into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
Modify `src/App.tsx` to replace `simulateValidation()` with real span-based validation:

1. Import validation engine:
   - Add import: `import { validateSpans } from './lib/validation'`
   - Add import: `import type { ValidationAttempt } from './lib/validation'`

2. Add attempt tracking state:
   - Add state: `const [attemptHistory, setAttemptHistory] = useState<Record<string, number>>({})`
   - This tracks attempts per validation rule for progressive hints

3. Modify `handleValidate` function:
   - Keep: setIsValidating(true), setWorkerError(null), updateProgress to 'in-progress'
   - Keep: await runCode(code) with try/catch
   - Replace: Remove the setTimeout with simulateValidation
   - Add: Call `const results = validateSpans(currentCase.phase1.validations, spans, attemptHistory)`
   - Add: Update attempt history for failed rules
     ```typescript
     results.forEach(r => {
       if (!r.passed) {
         setAttemptHistory(prev => ({
           ...prev,
           [r.description]: (prev[r.description] || 0) + 1
         }));
       }
     });
     ```
   - Keep: setValidationResults(results), check if all passed → setAppPhase('investigation')
   - Keep: setIsValidating(false)

4. Add effect to clear validation on code change:
   ```typescript
   useEffect(() => {
     setValidationResults([]);
   }, [code]);
   ```
   Per locked decision: Reset all validation results on every code edit

5. Remove: `simulateValidation()` function entirely

6. Ensure `handleCaseSolved` properly unlocks next case:
   - Verify it calls `updateProgress(nextCase.id, { status: 'available' })`
   - This is already implemented, just confirm it works with new flow

Use spans array from useCodeRunner hook. Validation runs automatically after code execution.
  </action>
  <verify>
    <automated>grep -q "validateSpans" src/App.tsx && grep -q "attemptHistory" src/App.tsx && ! grep -q "simulateValidation" src/App.tsx && echo "Integration complete"</automated>
    <manual>Review handleValidate function uses real span validation</manual>
  </verify>
  <done>
    - App.tsx imports and uses validateSpans() from lib/validation
    - attemptHistory state tracks per-rule attempts
    - simulateValidation() function removed
    - Validation clears when code changes
    - Next case unlocks on handleCaseSolved
  </done>
</task>

<task type="auto">
  <name>Update ValidationPanel with execution spinner</name>
  <files>src/components/ValidationPanel.tsx</files>
  <action>
Modify `src/components/ValidationPanel.tsx` to show spinner during code execution:

1. The component already receives `isValidating` prop - verify it handles both:
   - Worker initialization (!isWorkerReady)
   - Code execution (isValidating from running code)

2. Update the button/loading states:
   - When `isValidating` is true, show "Running code..." with spinner
   - The existing `<Loader2 className="w-4 h-4 animate-spin" />` pattern is good
   - Display "Running Python in WASM..." or similar during execution

3. Add execution feedback in results area:
   - When isValidating and results.length === 0, show:
     ```
     <div className="flex flex-col items-center justify-center text-slate-400">
       <Loader2 className="w-8 h-8 animate-spin mb-2" />
       <p className="text-sm">Running code and capturing telemetry...</p>
     </div>
     ```

4. Verify staggered animation still works:
   - The existing `animate-slide-in` with `animationDelay: ${index * 100}ms` should remain
   - Results should appear one by one as they validate

5. Celebration on complete (Claude's discretion):
   - When phaseUnlocked becomes true, could add subtle glow/pulse to the success message
   - Existing Sparkles icon and green styling is good foundation
   - Optional: Add confetti or similar if desired (or keep simple)

Ensure component is fully typed and no TypeScript errors.
  </action>
  <verify>
    <automated>grep -q "Running code" src/components/ValidationPanel.tsx && grep -q "animate-spin" src/components/ValidationPanel.tsx && echo "Panel updated"</automated>
    <manual>Review ValidationPanel shows spinner during execution</manual>
  </verify>
  <done>
    - ValidationPanel shows spinner with "Running code..." during execution
    - Staggered animation preserved for result reveals
    - Phase complete celebration visible (Sparkles + green styling)
  </done>
</task>

<task type="auto">
  <name>Add attempt count display to validation results</name>
  <files>src/components/ValidationPanel.tsx</files>
  <action>
Enhance `ValidationPanel.tsx` to show attempt counts for progressive feedback:

1. Update ValidationPanelProps to include attempt tracking:
   - Add optional prop: `attemptHistory?: Record<string, number>`
   - Or ensure ValidationResult already has attemptsOnThisRule from validation engine

2. Modify result display to show attempt indicator:
   - For failed validations, show small text like "Attempt N" 
   - Or use visual indicator (dots, progress) showing getting closer to guided help
   - Keep it subtle - don't overwhelm the user

3. Update message display:
   - The validation engine already selects appropriate message (hint vs guided)
   - Just ensure the message renders correctly in the existing UI
   - The `result.message` should already contain the right progressive hint

4. If guidedMessage is showing (attempt >= 3), make it visually distinct:
   - Maybe add "💡 Pro tip:" prefix or similar styling
   - Use amber/yellow tint instead of red for guided messages (more helpful than error)

Ensure the attemptsOnThisRule from ValidationResult is displayed usefully.
  </action>
  <verify>
    <automated>grep -q "attempt" src/components/ValidationPanel.tsx && echo "Attempt tracking added"</automated>
    <manual>Review ValidationPanel shows attempt indicators for failed validations</manual>
  </verify>
  <done>
    - Attempt count visible on failed validation items
    - Guided messages (3+ attempts) visually distinct from initial hints
    - Messages render correctly from validation engine
  </done>
</task>

</tasks>

<verification>
- User runs code, sees "Running code..." spinner
- Validation results appear with staggered animation
- Failed validations show attempt count and progressive hints
- All validations pass → Investigation tab unlocks with celebration
- Next case becomes available after solving current case
- Editing code clears validation results immediately
</verification>

<success_criteria>
- Validation checks real captured spans (not code strings)
- Spinner feedback during code execution
- Staggered reveal of validation results
- Progressive hints escalate based on attempt count
- Case unlocks properly on completion
- Code changes reset validation state
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-core-loop/02-02-SUMMARY.md`
</output>
